a=np.array([(1,3,4),(2,3,6),(5,8,90)])
print(np.where(a>3,520,1314))

排序：
import numpy as np
x = np.array([59, 29, 391)
a = np.argsort(x)
print(f'数组升序：(x[a])
和print（x.sort）有什么区别

如果需要保留原数组：使用 sorted_x = x[np.argsort(x)]

如果不需要原数组：使用 x.sort()

还可以使用 np.sort(x)，它返回排序后的新数组而不修改原数组
